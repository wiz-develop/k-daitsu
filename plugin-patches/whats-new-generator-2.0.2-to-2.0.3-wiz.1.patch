diff -urN a/whats-new-genarator/admin-ui.php b/whats-new-genarator/admin-ui.php
--- a/whats-new-genarator/admin-ui.php	2019-01-11 17:10:53
+++ b/whats-new-genarator/admin-ui.php	2026-08-29 22:20:40
@@ -1,7 +1,7 @@
 <?php
 
-class WNGAdminUi {
-	var $file_path;
+class WNGAdminUi {
+	public $file_path;
 
 	public function __construct( $path){
 		$this->file_path = $path;
@@ -21,8 +21,11 @@
 		add_settings_field('wng_latest_new', '最新記事にNEW!マークをつける', array(&$this,'setting_latest_new'), $this->file_path, 'main_section');
 	}
 
-	public function show_admin_page() {
-		$file = $this->file_path;
+	public function show_admin_page() {
+		if ( ! current_user_can( 'manage_options' ) ) {
+			wp_die( esc_html__( 'You do not have permission to access this page.' ) );
+		}
+		$file = $this->file_path;
 		$option_name = WNG::OPTIONS;
 		$shortcode = "[" . WNG::SHORTCODE . "]";
 		include_once( dirname(__FILE__) . '/admin-view.php');
@@ -30,17 +33,24 @@
 		include_once( dirname(__FILE__) . '/whatsnew-view.php');
 	}
 
-	function validate($input) {
-		if ( !is_numeric( $input['wng_number']) || $input['wng_number'] < 1 || $input['wng_number'] > 30){
-			$input['wng_number'] = 10;
-		}
-		if ( !is_numeric( $input['wng_newmark']) || $input['wng_newmark'] < 0 || $input['wng_newmark'] > 30){
-			$input['wng_newmark'] = 0;
-		}
-		$input['wng_title'] = esc_html( $input['wng_title'] );
-		$input['wng_category_name'] = trim(esc_html( $input['wng_category_name'] ));
-		$input['wng_postlist_url'] = esc_url( $input['wng_postlist_url'] );
-		return $input;
+	function validate($input) {
+		$input = is_array( $input ) ? $input : array();
+		$number = isset( $input['wng_number'] ) ? absint( $input['wng_number'] ) : 10;
+		$newmark = isset( $input['wng_newmark'] ) ? absint( $input['wng_newmark'] ) : 0;
+		$title_tag = isset( $input['wng_title_tag'] ) ? $input['wng_title_tag'] : 'h1';
+		$content_type = isset( $input['wng_content_type'] ) ? $input['wng_content_type'] : '投稿';
+		$orderby = isset( $input['wng_orderby'] ) ? $input['wng_orderby'] : '公開日順';
+
+		return array(
+			'wng_title'         => isset( $input['wng_title'] ) ? sanitize_text_field( $input['wng_title'] ) : '',
+			'wng_title_tag'     => in_array( $title_tag, array( 'h1', 'h2', 'h3', 'h4', 'p' ), true ) ? $title_tag : 'h1',
+			'wng_content_type'  => in_array( $content_type, array( '投稿', '固定ページ', '投稿＋固定ページ' ), true ) ? $content_type : '投稿',
+			'wng_category_name' => isset( $input['wng_category_name'] ) ? sanitize_title( $input['wng_category_name'] ) : '',
+			'wng_orderby'       => in_array( $orderby, array( '公開日順', '更新日順' ), true ) ? $orderby : '公開日順',
+			'wng_number'        => min( 30, max( 1, $number ) ),
+			'wng_newmark'       => min( 30, $newmark ),
+			'wng_latest_new'    => ! empty( $input['wng_latest_new'] ),
+		);
 	}
 
 	function  section_text_fn() {
@@ -49,7 +59,7 @@
 	function setting_title() {
 		$options = WNG::get_option();
 		$value = $options["wng_title"];
-		echo "<input id='wng_title' name='whats_new_options[wng_title]' size='40' type='text' value='{$value}' />";
+		printf( "<input id='wng_title' name='whats_new_options[wng_title]' size='40' type='text' value='%s' />", esc_attr( $value ) );
 	}
 
 	function setting_title_tag() {
@@ -58,7 +68,7 @@
 		echo "<select id='wng_title_tag' name='whats_new_options[wng_title_tag]'>";
 		foreach($items as $item) {
 			$selected = ($options['wng_title_tag']==$item) ? 'selected="selected"' : '';
-			echo "<option value='$item' $selected>$item</option>";
+			printf( '<option value="%1$s" %2$s>%1$s</option>', esc_attr( $item ), $selected );
 		}
 		echo "</select>";
 	}
@@ -72,25 +82,25 @@
 	function setting_newmark() {
 		$options = WNG::get_option();
 		$value = $options["wng_newmark"];
-		echo "<input id='wng_newmark' name='whats_new_options[wng_newmark]' size='2' type='text' value='{$value}' />日間";
+		printf( "<input id='wng_newmark' name='whats_new_options[wng_newmark]' size='2' type='number' min='0' max='30' value='%s' />日間", esc_attr( $value ) );
 	}
 
 	function setting_latest_new() {
 		$options = WNG::get_option();
 		$checked = (isset($options["wng_latest_new"]) && $options["wng_latest_new"]) ? $checked = ' checked="checked" ': "";
-		echo "<input id='wng_latest_new' name='whats_new_options[wng_latest_new]' type='checkbox' {$checked} />";
+		echo "<input id='wng_latest_new' name='whats_new_options[wng_latest_new]' type='checkbox' value='1' {$checked} />";
 	}
 
 	function setting_category_name() {
 		$options = WNG::get_option();
 		$value = $options["wng_category_name"];
-		echo "<input id='wng_category_name' name='whats_new_options[wng_category_name]' size='40' type='text' value='{$value}' />";
+		printf( "<input id='wng_category_name' name='whats_new_options[wng_category_name]' size='40' type='text' value='%s' />", esc_attr( $value ) );
 	}
 
 	function  setting_number() {
 		$options = WNG::get_option();
 		$value = $options["wng_number"];
-		echo "<input id='wng_number' name='whats_new_options[wng_number]' size='2' type='text' value='{$value}' />個";
+		printf( "<input id='wng_number' name='whats_new_options[wng_number]' size='2' type='number' min='1' max='30' value='%s' />個", esc_attr( $value ) );
 	}
 
 	function setting_content_type() {
@@ -98,7 +108,7 @@
 		$items = array("投稿", "固定ページ", "投稿＋固定ページ");
 		foreach($items as $item) {
 			$checked = ($options['wng_content_type']==$item) ? ' checked="checked" ' : '';
-			echo "<label><input {$checked} value='{$item}' name='whats_new_options[wng_content_type]' type='radio' /> $item</label><br />";
+			printf( '<label><input %1$s value="%2$s" name="whats_new_options[wng_content_type]" type="radio" /> %3$s</label><br />', $checked, esc_attr( $item ), esc_html( $item ) );
 		}
 	}
 
@@ -107,7 +117,7 @@
 		$items = array("公開日順", "更新日順");
 		foreach($items as $item) {
 			$checked = ($options['wng_orderby']==$item) ? ' checked="checked" ' : '';
-			echo "<label><input {$checked} value='{$item}' name='whats_new_options[wng_orderby]' type='radio' /> $item</label><br />";
+			printf( '<label><input %1$s value="%2$s" name="whats_new_options[wng_orderby]" type="radio" /> %3$s</label><br />', $checked, esc_attr( $item ), esc_html( $item ) );
 		}
 	}
 }
diff -urN a/whats-new-genarator/admin-view.php b/whats-new-genarator/admin-view.php
--- a/whats-new-genarator/admin-view.php	2019-01-11 17:10:53
+++ b/whats-new-genarator/admin-view.php	2026-08-29 22:20:40
@@ -6,7 +6,7 @@
 	<h3>ショートコード</h3>
 	<p>以下のコードをコピーして、What's Newを表示する固定ページや投稿の本文内に貼り付けてください。</p>
 	<p>
-		<input type="text" value=<?php echo $shortcode;?> readonly />
+		<input type="text" value="<?php echo esc_attr( $shortcode ); ?>" readonly />
 	</p>
 	<form action="options.php" method="post">
 		<?php settings_fields( $option_name ); ?>
diff -urN a/whats-new-genarator/readme.txt b/whats-new-genarator/readme.txt
--- a/whats-new-genarator/readme.txt	2019-01-11 17:10:53
+++ b/whats-new-genarator/readme.txt	2026-08-29 22:21:01
@@ -2,9 +2,10 @@
 Contributors: WP-BizPlugin
 Donate link:
 Tags: what's new, update Post page
-Requires at least: 4.0
-Tested up to: 4.9.1
-Stable tag: 2.0.2
+Requires at least: 6.7
+Requires PHP: 7.4
+Tested up to: 7.1
+Stable tag: 2.0.3-wiz.1
 License: GPLv2 or later
 License URI: http://www.gnu.org/licenses/gpl-2.0.html
 
@@ -37,9 +38,13 @@
 1. Latest information
 2. Admin page
 
-== Changelog ==
-
-= 2.0.2 =
+== Changelog ==
+
+= 2.0.3-wiz.1 =
+* PHP and current WordPress compatibility fixes.
+* Hardened settings validation, permissions, and output escaping.
+
+= 2.0.2 =
 * 軽微な不具合を修正しました
 
 = 2.0.1 =
diff -urN a/whats-new-genarator/whats-new-generator.php b/whats-new-genarator/whats-new-generator.php
--- a/whats-new-genarator/whats-new-generator.php	2019-01-11 17:10:53
+++ b/whats-new-genarator/whats-new-generator.php	2026-08-29 22:20:40
@@ -3,23 +3,24 @@
  Plugin Name: What's New Generator
 Plugin URI: http://residentbird.main.jp/bizplugin/
 Description: What's New(新着情報)を指定した固定ページや投稿に自動的に表示するプラグインです。
-Version: 2.0.2
+Version: 2.0.3-wiz.1
 Author:Hideki Tanaka
 Author URI: http://residentbird.main.jp/bizplugin/
 Text Domain: whats-new-genarator
 */
 
-include_once( dirname(__FILE__) . "/admin-ui.php" );
+include_once dirname( __FILE__ ) . '/admin-ui.php';
 new WhatsNewPlugin();
 
 class WNG
 {
-	const VERSION = "2.0.2";
+	const VERSION = '2.0.3-wiz.1';
 	const SHORTCODE = "showwhatsnew";
 	const OPTIONS = "whats_new_options";
 
-	public static function get_option(){
-		return get_option(self::OPTIONS);
+	public static function get_option(){
+		$options = get_option( self::OPTIONS, array() );
+		return is_array( $options ) ? $options : array();
 	}
 
 	public static function update_option( $options ){
@@ -38,10 +39,10 @@
 	}
 }
 
-class WhatsNewPlugin{
+class WhatsNewPlugin {
+
+	public $adminUi;
 
-	var $adminUi;
-
 	public function __construct(){
 		register_activation_hook(__FILE__, array(&$this,'on_activation'));
 		add_action( 'admin_init', array(&$this,'on_admin_init') );
@@ -73,7 +74,7 @@
 	}
 
 	public function on_admin_menu() {
-		add_options_page("What's New 設定", "What's New 設定", 'administrator', __FILE__, array(&$this->adminUi, 'show_admin_page'));
+		add_options_page( "What's New 設定", "What's New 設定", 'manage_options', __FILE__, array( &$this->adminUi, 'show_admin_page' ) );
 	}
 
 	function on_enqueue_css_js() {
@@ -88,7 +89,7 @@
 		include( dirname(__FILE__) . '/whatsnew-view.php');
 	}
 
-	function show_shortcode(){
+	function show_shortcode( $attributes = array() ) {
 		ob_start();
 		$this->show_whatsnew();
 		$contents = ob_get_contents();
@@ -97,27 +98,31 @@
 	}
 }
 
-class WhatsNewInfo{
-	var $title;
-	var $items = array();
+class WhatsNewInfo {
+	public $title = '';
+	public $title_tag = 'h1';
+	public $items = array();
 
 	public function __construct(){
 		$options = WNG::get_option();
-		$this->title = esc_html( $options['wng_title'] );
-		$this->title_tag = $options['wng_title_tag'];
+		$this->title = isset( $options['wng_title'] ) ? sanitize_text_field( $options['wng_title'] ) : '';
+		$title_tag = isset( $options['wng_title_tag'] ) ? $options['wng_title_tag'] : 'h1';
+		$this->title_tag = in_array( $title_tag, array( 'h1', 'h2', 'h3', 'h4', 'p' ), true ) ? $title_tag : 'h1';
 
 		$condition = array();
-		if ( $options['wng_content_type'] == '投稿'){
-			$condition['post_type'] = 'post';
-		}else if ( $options['wng_content_type'] == '固定ページ' ){
-			$condition['post_type'] = 'page';
-		}else{
-			$condition['post_type'] = array('page', 'post');
-		}
-		$condition['numberposts'] = $options['wng_number'];
-		$condition['order'] = 'desc';
-		$condition['orderby'] = $options['wng_orderby'] == '公開日順' ? 'post_date' : 'modified';
-		$condition['category_name'] = $options['wng_category_name'];
+		$content_type = isset( $options['wng_content_type'] ) ? $options['wng_content_type'] : '投稿';
+		if ( $content_type === '投稿' ) {
+			$condition['post_type'] = 'post';
+		} elseif ( $content_type === '固定ページ' ) {
+			$condition['post_type'] = 'page';
+		}else{
+			$condition['post_type'] = array('page', 'post');
+		}
+		$condition['numberposts'] = isset( $options['wng_number'] ) ? min( 30, max( 1, absint( $options['wng_number'] ) ) ) : 10;
+		$condition['order'] = 'desc';
+		$condition['orderby'] = ! isset( $options['wng_orderby'] ) || $options['wng_orderby'] === '公開日順' ? 'post_date' : 'modified';
+		$condition['category_name'] = isset( $options['wng_category_name'] ) ? sanitize_title( $options['wng_category_name'] ) : '';
+		$condition['post_status'] = 'publish';
 
 		$posts = get_posts( $condition );
 		if ( !is_array($posts) ){
@@ -129,20 +134,20 @@
 	}
 }
 
-class WhatsNewItem{
-	var $date;
-	var $raw_date;
-	var $title;
-	var $url;
-	var $newmark;
+class WhatsNewItem {
+	public $date;
+	public $raw_date;
+	public $title;
+	public $url;
+	public $newmark;
 	private static $number = 0;
 
 	public function __construct( $post ){
 		$options = WNG::get_option();
-		$orderby = $options['wng_orderby'];
-		$this->raw_date = $orderby == '公開日順' ? $post->post_date : $post->post_modified;
-		$this->date = date(get_option('date_format'), strtotime($this->raw_date));
-		$this->title = esc_html( $post->post_title );
+		$orderby = isset( $options['wng_orderby'] ) ? $options['wng_orderby'] : '公開日順';
+		$this->raw_date = $orderby === '公開日順' ? $post->post_date : $post->post_modified;
+		$this->date = wp_date( get_option( 'date_format' ), strtotime( $this->raw_date ) );
+		$this->title = $post->post_title;
 		$this->url = get_permalink($post->ID);
 		$this->newmark = $this->is_new();
 		self::$number++;
@@ -153,16 +158,16 @@
 		if ( isset($options['wng_latest_new']) && self::$number == 0){
 			return true;
 		}
-		$term = $options['wng_newmark'];
-		if ( !isset($term) || $term == 0){
-			return false;
-		}
-		$today = date_i18n('U');
-		$post_date = date('U', strtotime($this->raw_date));
+		$term = isset( $options['wng_newmark'] ) ? absint( $options['wng_newmark'] ) : 0;
+		if ( 0 === $term ) {
+			return false;
+		}
+		$today = current_time( 'timestamp' );
+		$post_date = strtotime( $this->raw_date );
 		$diff = ( $today - $post_date ) / ( 24 * 60 * 60 );
 		if ($term > $diff){
 			return true;
 		}
 		return false;
 	}
-}
\ No newline at end of file
+}
diff -urN a/whats-new-genarator/whatsnew-view.php b/whats-new-genarator/whatsnew-view.php
--- a/whats-new-genarator/whatsnew-view.php	2019-01-11 17:10:53
+++ b/whats-new-genarator/whatsnew-view.php	2026-08-29 22:21:01
@@ -1,20 +1,20 @@
 <div class='whatsnew'>
 	<?php if ( $info->title ): ?>
-		<?php echo '<'.$info->title_tag.'>'.$info->title.'</'.$info->title_tag.'>'; ?>
+		<?php printf( '<%1$s>%2$s</%1$s>', esc_attr( $info->title_tag ), esc_html( $info->title ) ); ?>
 	<?php endif; ?>
 
 	<hr/>
 	<?php foreach($info->items as $item): ?>
 	<dl>
-		<a href="<?php echo $item->url; ?>">
+		<a href="<?php echo esc_url( $item->url ); ?>">
 		<dt>
-			<?php echo $item->date; ?>
+			<?php echo esc_html( $item->date ); ?>
 		</dt>
 		<dd>
 			<?php if ( $item->newmark ): ?>
 			<span class='newmark'>NEW!</span>
 			<?php endif; ?>
-			<?php echo $item->title; ?>
+			<?php echo esc_html( $item->title ); ?>
 		</dd>
 		</a>
 	</dl>
